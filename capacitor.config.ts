import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.lavanderia.melipilla',
  appName: 'Lavanderia Melipilla',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
